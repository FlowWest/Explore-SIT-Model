migratory_survivalUI <- function(id) {
  ns <- NS(id)
  
  tagList(
    fluidRow(
      column(width = )
    )
  )
  
}

migratory_survival <- function(input, output, session) {
  
}

Juv.OUTM.S(Q.cms, aveT, tot.div, prop.div)
